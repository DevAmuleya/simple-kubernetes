apiVersion: v1
kind: Service
metadata:
  name: simple-kubernetes-service
spec:
  type: NodePort
  selector:
    app: simple-kubernetes
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30007