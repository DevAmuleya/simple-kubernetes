apiVersion: v1
kind: Service
metadata:
  name: simple-kubernetes
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: simple-kubernetes

  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
